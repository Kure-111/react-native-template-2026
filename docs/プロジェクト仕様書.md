# プロジェクト仕様書

## プロジェクト概要

このプロジェクトは、React Native Expo テンプレートです。
ここからフォークして新しいプロジェクトを開発できるように設計されています。

### 技術スタック

- **フロントエンド:** React Native (Expo)
- **バックエンド:** Supabase
- **その他:** Google Apps Script（必要に応じて）
- **使用言語:** JavaScript（TypeScript ではない）

---

## デモアプリ

テンプレートには、シンプルなカウンターアプリが含まれています。

### 機能

- カウントアップボタン
- カウントダウンボタン
- リセットボタン
- カウント値の表示

---

## ディレクトリ構造

```
src/
├── features/
│   ├── auth/
│   │   └── screens/
│   │       └── LoginScreen.jsx
│   ├── item1/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item1Screen.jsx
│   │   └── constants.js
│   ├── item2/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item2Screen.jsx
│   │   └── constants.js
│   ├── item3/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item3Screen.jsx
│   │   └── constants.js
│   ├── item4/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item4Screen.jsx
│   │   └── constants.js
│   ├── item5/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item5Screen.jsx
│   │   └── constants.js
│   ├── item6/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item6Screen.jsx
│   │   └── constants.js
│   ├── item7/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item7Screen.jsx
│   │   └── constants.js
│   ├── item8/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item8Screen.jsx
│   │   └── constants.js
│   ├── item9/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── Item9Screen.jsx
│   │   └── constants.js
│   ├── item10/                          # 実長システム
│   │   ├── components/                  # UI コンポーネント
│   │   │   ├── VisitorCounter.jsx       # 来場者カウンター表示（表示のみ）
│   │   │   ├── CountHistoryChart.jsx    # 時間帯別グラフ
│   │   │   ├── SuspiciousPersonCard.jsx # 不審者情報カード
│   │   │   ├── SuspiciousPersonList.jsx # 不審者リスト
│   │   │   ├── SuspiciousPersonForm.jsx # 不審者登録フォーム（Google Drive連携）
│   │   │   ├── NotificationPopup.jsx    # 通知送信ポップアップ
│   │   │   ├── WeatherRadar.jsx         # 雨雲レーダマップ
│   │   │   ├── WeatherInfo.jsx          # 天気情報表示（降水量mm/h含む）
│   │   │   ├── DigitalClock.jsx         # デジタル時計
│   │   │   ├── EmergencyModeToggle.jsx  # 緊急モードスイッチ
│   │   │   ├── EmergencyTypeSelector.jsx # 自然災害種別選択
│   │   │   ├── EmergencyMessageInput.jsx # 詳細メッセージ入力
│   │   │   ├── EmergencyNotificationPopup.jsx # 緊急通知ポップアップ
│   │   │   ├── EvacuationInfoScreen.jsx # 避難情報画面
│   │   │   ├── SecurityMap.jsx          # 警備員配置マップ（未定）
│   │   │   ├── SecurityList.jsx         # 警備員リスト（未定）
│   │   │   ├── SecurityMemberCard.jsx   # 警備員情報カード（未定）
│   │   │   └── AreaSummary.jsx          # エリア別集計表示（未定）
│   │   ├── hooks/                       # カスタムフック
│   │   │   ├── useVisitorCount.js       # 来場者カウント管理（表示のみ）
│   │   │   ├── useSuspiciousPersons.js  # 不審者情報管理
│   │   │   ├── useWeatherData.js        # 天気データ取得（降水量含む）
│   │   │   ├── useEmergencyMode.js      # 緊急モード管理（自然災害のみ）
│   │   │   ├── useNotificationPopup.js  # 通知ポップアップ管理
│   │   │   ├── useSecurityPlacements.js # 警備配置管理（未定）
│   │   │   └── useRealtimeSync.js       # リアルタイム同期
│   │   ├── screens/                     # 画面
│   │   │   ├── Item10Screen.jsx         # メイン画面（統合画面）
│   │   │   ├── VisitorCounterScreen.jsx # 来場者数単独画面（開発用）
│   │   │   ├── SuspiciousPersonScreen.jsx # 不審者情報単独画面（開発用）
│   │   │   ├── SuspiciousPersonDetailScreen.jsx  # 不審者詳細画面
│   │   │   ├── WeatherRadarScreen.jsx   # 雨雲レーダ単独画面（開発用）
│   │   │   ├── EmergencyModeScreen.jsx  # 緊急モード単独画面（開発用）
│   │   │   ├── EmergencyHistoryScreen.jsx # 緊急履歴画面
│   │   │   ├── EvacuationInfoScreen.jsx # 避難情報画面
│   │   │   └── SecurityMemberDetailScreen.jsx # 警備員詳細画面（未定）
│   │   ├── services/                    # サービス層
│   │   │   ├── visitorCountService.js   # 来場者カウントAPI
│   │   │   ├── suspiciousPersonService.js # 不審者情報API（Google Drive連携）
│   │   │   ├── weatherService.js        # 天気API連携（降水量取得）
│   │   │   ├── emergencyService.js      # 緊急モードAPI
│   │   │   ├── securityService.js       # 警備配置API（未定）
│   │   │   └── notificationService.js   # 通知サービス（将来的にERP連携）
│   │   └── constants.js                 # 定数定義
│   ├── jimu-shift/
│   │   ├── assets/
│   │   │   ├── ANNOUNCEMENTS.md
│   │   │   ├── 謎解き.jpeg
│   │   │   ├── 渉外部B館前.jpeg
│   │   │   ├── ステッカー.jpeg
│   │   │   ├── 渉外部西門.jpeg
│   │   │   └── 渉外部人工芝.jpeg
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── screens/
│   │   │   └── JimuShiftScreen.jsx
│   │   ├── services/
│   │   │   └── shiftService.js
│   │   └── constants.js
│   └── settings/
│       ├── components/
│       │   ├── ThemeOptionRow.jsx
│       │   └── ThemeStatusText.jsx
│       ├── screens/
│       │   └── SettingsThemeScreen.jsx
│       └── constants.js
├── navigation/
│   ├── components/
│   │   └── CustomDrawerContent.jsx
│   ├── AppNavigator.jsx
│   └── DrawerNavigator.jsx
├── services/
│   ├── gas/
│   │   └── gasApi.js
│   └── supabase/
│       ├── authService.js
│       ├── client.js
│       ├── permissionService.js
│       └── userService.js
└── shared/
    ├── components/
    │   ├── PlaceholderContent.jsx
    │   ├── ScreenErrorBoundary.jsx
    │   └── ThemedHeader.jsx
    ├── constants/
    │   └── index.js
    ├── contexts/
    │   ├── AuthContext.js
    │   └── ThemeContext.jsx
    ├── hooks/
    │   └── useTheme.js
    ├── services/
    │   └── themeSettingsService.js
    └── utils/
        ├── serviceWorker.js
        ├── themeTokens.js
        └── validation.js
```



---

## Item10 ディレクトリ構造の詳細

### components/ - UI コンポーネント

**来場者カウンター関連:**
- `VisitorCounter.jsx` - 現在の来場者数を大きく表示するコンポーネント（表示のみ）
- `CountHistoryChart.jsx` - 時間帯別の来場者数をグラフ表示

**不審者情報関連:**
- `SuspiciousPersonCard.jsx` - 1件の不審者情報を表示するカードUI
- `SuspiciousPersonList.jsx` - 不審者情報一覧を表示するリストコンポーネント
- `SuspiciousPersonForm.jsx` - 不審者情報を新規登録するフォーム（Google Drive連携）
- `NotificationPopup.jsx` - 通知送信ポップアップ（送信先・内容プレビュー表示）

**天気情報関連:**
- `WeatherRadar.jsx` - 雨雲レーダマップを表示（react-native-maps使用）
- `WeatherInfo.jsx` - 降水確率・天気予報・警報・降水量（mm/h）を表示

**時間表示関連:**
- `DigitalClock.jsx` - 現在時刻と日付をデジタル表示

**緊急モード関連:**
- `EmergencyModeToggle.jsx` - 緊急モードのON/OFF切替スイッチ
- `EmergencyTypeSelector.jsx` - 自然災害の種類選択（プルダウン）
- `EmergencyMessageInput.jsx` - 詳細メッセージ入力欄
- `EmergencyNotificationPopup.jsx` - 緊急通知ポップアップ（送信先・内容プレビュー・避難情報説明）
- `EvacuationInfoScreen.jsx` - 避難情報表示画面（災害種別ごとに内容変更）

**警備配置関連（詳細仕様未定）:**
- `SecurityMap.jsx` - 警備員の配置場所をマップ表示
- `SecurityList.jsx` - 警備員情報をリスト形式で表示
- `SecurityMemberCard.jsx` - 1人の警備員情報を表示するカード（無線チャンネル情報含む）
- `AreaSummary.jsx` - エリア別の警備員数を集計表示

---

### hooks/ - カスタムフック

**useVisitorCount.js**
```javascript
// 来場者カウント情報を取得（表示のみ）
export const useVisitorCount = () => {
  const [count, setCount] = useState(0);
  const [history, setHistory] = useState([]);
  
  const fetchCurrent = async () => { /* 現在のカウント取得 */ };
  const fetchHistory = async () => { /* 履歴取得 */ };
  
  // リアルタイム同期
  useEffect(() => {
    const subscription = supabase
      .channel('visitor_counts')
      .on('postgres_changes', { event: '*', schema: 'public', table: 'visitor_counts' }, 
        () => fetchCurrent())
      .subscribe();
    return () => subscription.unsubscribe();
  }, []);
  
  return { count, history, fetchCurrent, fetchHistory };
};
```

**useSuspiciousPersons.js**
```javascript
// 不審者情報の取得・登録・更新を管理
export const useSuspiciousPersons = () => {
  const [persons, setPersons] = useState([]);
  const [loading, setLoading] = useState(false);
  const [showNotificationPopup, setShowNotificationPopup] = useState(false);
  const [notificationData, setNotificationData] = useState(null);
  
  const fetchPersons = async () => { /* 一覧取得 */ };
  const addPerson = async (data, photoFile) => { 
    /* Google Drive に写真アップロード */
    /* Supabase に情報保存 */
    /* 通知ポップアップを表示 */
    setNotificationData({
      to: '全スタッフ',
      message: `${data.location}で不審者が発見されました`,
    });
    setShowNotificationPopup(true);
  };
  const updateStatus = async (id, status) => { /* ステータス更新 */ };
  
  return { 
    persons, 
    loading, 
    showNotificationPopup, 
    notificationData,
    setShowNotificationPopup,
    fetchPersons, 
    addPerson, 
    updateStatus 
  };
};
```

**useWeatherData.js**
```javascript
// 天気情報APIからデータ取得（降水量含む）
export const useWeatherData = (latitude, longitude) => {
  const [weather, setWeather] = useState(null);
  const [forecast, setForecast] = useState([]);
  const [rainfall, setRainfall] = useState(0); // mm/h
  
  const fetchWeather = async () => { 
    /* 現在の天気取得 */
    /* 降水量（mm/h）も取得 */
  };
  const fetchForecast = async () => { /* 予報取得 */ };
  
  useEffect(() => {
    fetchWeather();
    const interval = setInterval(fetchWeather, 300000); // 5分ごと
    return () => clearInterval(interval);
  }, [latitude, longitude]);
  
  return { weather, forecast, rainfall, refetch: fetchWeather };
};
```

**useEmergencyMode.js**
```javascript
// 緊急モードの状態管理（自然災害のみ）
export const useEmergencyMode = () => {
  const [isEmergency, setIsEmergency] = useState(false);
  const [disasterType, setDisasterType] = useState('');
  const [message, setMessage] = useState('');
  const [history, setHistory] = useState([]);
  const [showNotificationPopup, setShowNotificationPopup] = useState(false);
  const [notificationData, setNotificationData] = useState(null);
  
  const activate = async (type, customMessage) => { 
    /* 緊急モード発動 */
    /* 通知ポップアップを表示 */
    setNotificationData({
      to: '全員',
      message: `【緊急】${type}：${customMessage}`,
      additionalInfo: '全員のERP画面が避難情報画面に切り替わります',
    });
    setShowNotificationPopup(true);
  };
  const deactivate = async () => { 
    /* 緊急モード解除 */
    /* 解除通知ポップアップ表示 */
  };
  const fetchHistory = async () => { /* 履歴取得 */ };
  
  return { 
    isEmergency, 
    disasterType, 
    message, 
    history,
    showNotificationPopup,
    notificationData,
    setShowNotificationPopup,
    setDisasterType,
    setMessage,
    activate, 
    deactivate, 
    fetchHistory 
  };
};
```

**useNotificationPopup.js**
```javascript
// 通知ポップアップの管理
export const useNotificationPopup = () => {
  const [isVisible, setIsVisible] = useState(false);
  const [notificationData, setNotificationData] = useState(null);
  
  const showPopup = (data) => {
    setNotificationData(data);
    setIsVisible(true);
  };
  
  const hidePopup = () => {
    setIsVisible(false);
    setNotificationData(null);
  };
  
  const handleSend = async () => {
    // 現在は未実装
    // 将来的にERP通知システムを呼び出す
    console.log('通知送信（未実装）:', notificationData);
    hidePopup();
  };
  
  return { 
    isVisible, 
    notificationData, 
    showPopup, 
    hidePopup, 
    handleSend 
  };
};
```

**useSecurityPlacements.js**
```javascript
// 警備員配置情報の管理（無線チャンネル情報含む）
// 注意：詳細仕様は未定（今後検討）
export const useSecurityPlacements = () => {
  const [placements, setPlacements] = useState([]);
  const [viewMode, setViewMode] = useState('map'); // 'map' or 'list'
  
  const fetchPlacements = async () => { 
    /* 配置情報取得（無線チャンネル含む） */ 
  };
  const updateStatus = async (id, status) => { /* ステータス更新 */ };
  const updateLocation = async (id, lat, lon) => { /* 位置更新 */ };
  
  return { placements, viewMode, setViewMode, fetchPlacements, updateStatus, updateLocation };
};
```

**useRealtimeSync.js**
```javascript
// Supabase Realtimeでデータ同期
export const useRealtimeSync = (table, callback) => {
  useEffect(() => {
    const subscription = supabase
      .channel(`public:${table}`)
      .on('postgres_changes', { event: '*', schema: 'public', table }, callback)
      .subscribe();
    
    return () => subscription.unsubscribe();
  }, [table]);
};
```

---

### screens/ - 画面コンポーネント

**Item10Screen.jsx**
- メイン画面：すべての機能を統合表示
- ScrollViewで縦スクロール可能
- 各セクションをコンポーネント化して配置

**開発用単独画面（フェーズ1で使用）:**
- `VisitorCounterScreen.jsx` - 来場者数機能の単独画面
- `SuspiciousPersonScreen.jsx` - 不審者情報機能の単独画面
- `WeatherRadarScreen.jsx` - 雨雲レーダ機能の単独画面
- `EmergencyModeScreen.jsx` - 緊急モード機能の単独画面

**詳細画面:**
- `SuspiciousPersonDetailScreen.jsx` - 不審者情報の詳細画面（Google Drive からの写真表示、ステータス変更、マップ表示）
- `EmergencyHistoryScreen.jsx` - 緊急モードの発動履歴一覧（発動日時、発動者、自然災害の種類、詳細メッセージを表示）
- `EvacuationInfoScreen.jsx` - 緊急モード時に全員に表示される避難情報画面（自然災害の種類ごとに表示内容を変更：地震/台風/大雨・洪水）
- `SecurityMemberDetailScreen.jsx` - 警備員の詳細情報画面（無線チャンネル、現在地、ステータス表示、詳細仕様未定）

---

### services/ - サービス層（API通信）

**visitorCountService.js**
```javascript
import { supabase } from '../../../services/supabase/client';

export const visitorCountService = {
  // 現在のカウント取得
  getCurrent: async () => {
    const { data, error } = await supabase
      .from('visitor_counts')
      .select('*')
      .order('counted_at', { ascending: false })
      .limit(1);
    return { data: data?.[0], error };
  },
  
  // カウント更新
  updateCount: async (count, operation, userId) => {
    const { data, error } = await supabase
      .from('visitor_counts')
      .insert({
        count,
        counted_at: new Date().toISOString(),
        operation,
        updated_by: userId,
      });
    return { data, error };
  },
  
  // 時間帯別履歴取得
  getHistory: async (date) => {
    const { data, error } = await supabase
      .from('visitor_counts')
      .select('*')
      .gte('counted_at', `${date}T00:00:00Z`)
      .lte('counted_at', `${date}T23:59:59Z`)
      .order('counted_at', { ascending: true });
    return { data, error };
  },
};
```

**suspiciousPersonService.js**
```javascript
import { supabase } from '../../../services/supabase/client';
import { uploadToGoogleDrive } from '../../../services/googleDrive'; // Google Drive連携

export const suspiciousPersonService = {
  // 一覧取得
  getAll: async (limit = 5) => {
    const { data, error } = await supabase
      .from('suspicious_persons')
      .select('*')
      .order('discovered_at', { ascending: false })
      .limit(limit);
    return { data, error };
  },
  
  // 新規登録
  create: async (personData, photoFile) => {
    let photoUrl = null;
    
    // Google Driveに写真アップロード
    if (photoFile) {
      photoUrl = await uploadToGoogleDrive(photoFile, 'suspicious_persons');
      // photoUrl は Google Drive の共有リンク
    }
    
    // Supabase には Google Drive のリンクのみを保存
    const { data, error } = await supabase
      .from('suspicious_persons')
      .insert({ ...personData, photo_url: photoUrl });
    
    // 通知はポップアップで処理（サービス層では通知送信しない）
    // 将来的にERP通知システムが完成したら notificationService.js を呼び出す
    
    return { data, error };
  },
  
  // ステータス更新
  updateStatus: async (id, status) => {
    const updates = { status, updated_at: new Date().toISOString() };
    if (status === 'resolved') {
      updates.resolved_at = new Date().toISOString();
    }
    const { data, error } = await supabase
      .from('suspicious_persons')
      .update(updates)
      .eq('id', id);
    return { data, error };
  },
};
```

**weatherService.js**
```javascript
const API_KEY = process.env.OPENWEATHERMAP_API_KEY;
const BASE_URL = 'https://api.openweathermap.org/data/2.5';

export const weatherService = {
  // 現在の天気取得（降水量含む）
  getCurrentWeather: async (lat, lon) => {
    const response = await fetch(
      `${BASE_URL}/weather?lat=${lat}&lon=${lon}&appid=${API_KEY}&units=metric&lang=ja`
    );
    const data = await response.json();
    
    // 降水量（mm/h）を取得
    const rainfall = data.rain?.['1h'] || 0;
    
    return {
      ...data,
      rainfall, // mm/h
    };
  },
  
  // 予報取得
  getForecast: async (lat, lon) => {
    const response = await fetch(
      `${BASE_URL}/forecast?lat=${lat}&lon=${lon}&appid=${API_KEY}&units=metric&lang=ja`
    );
    return await response.json();
  },
};
```

**emergencyService.js**
```javascript
import { supabase } from '../../../services/supabase/client';

export const emergencyService = {
  // 緊急モード発動（自然災害のみ）
  activate: async (userId, disasterType, message) => {
    const { data, error } = await supabase
      .from('emergency_logs')
      .insert({
        action: 'activate',
        activated_by: userId,
        emergency_type: disasterType, // 地震/台風/大雨・洪水/その他
        reason: message,
      });
    
    // 通知はポップアップで処理（サービス層では通知送信しない）
    // 将来的にERP通知システムが完成したら notificationService.js を呼び出す
    
    return { data, error };
  },
  
  // 緊急モード解除
  deactivate: async (userId) => {
    const { data, error } = await supabase
      .from('emergency_logs')
      .insert({
        action: 'deactivate',
        activated_by: userId,
      });
    
    // 解除通知もポップアップで処理
    
    return { data, error };
  },
  
  // 履歴取得
  getHistory: async (limit = 10) => {
    const { data, error } = await supabase
      .from('emergency_logs')
      .select('*')
      .order('created_at', { ascending: false })
      .limit(limit);
    return { data, error };
  },
};
```

**securityService.js**
```javascript
import { supabase } from '../../../services/supabase/client';

// 注意：警備配置情報の詳細仕様は未定（今後検討）
export const securityService = {
  // 配置情報取得（警備員情報含む、無線チャンネル情報も）
  getPlacements: async () => {
    const { data, error } = await supabase
      .from('security_placements')
      .select('*, security_members(*)')
      .eq('status', 'on_duty');
    return { data, error };
  },
  
  // ステータス更新
  updateStatus: async (id, status, lat, lon) => {
    const { data, error } = await supabase
      .from('security_placements')
      .update({
        status,
        latitude: lat,
        longitude: lon,
        updated_at: new Date().toISOString(),
      })
      .eq('id', id);
    return { data, error };
  },
};
```

**notificationService.js**
```javascript
// 通知サービス（将来的にERP通知システムと連携）
// 現在は未実装

export const notificationService = {
  // 全員に通知送信（現在は未実装）
  sendToAll: async (notification) => {
    // 将来的にERPの通知APIを呼び出す
    // 現在はログのみ
    console.log('通知送信（未実装）:', notification);
    
    // return await fetch('/api/erp/notifications/send-all', {
    //   method: 'POST',
    //   headers: { 'Content-Type': 'application/json' },
    //   body: JSON.stringify(notification),
    // });
  },
  
  // ERP画面切替（現在は未実装）
  switchToEvacuationScreen: async (disasterType) => {
    // 将来的にERPのAPIを呼び出して全員の画面を切り替える
    console.log('画面切替（未実装）:', disasterType);
    
    // return await fetch('/api/erp/emergency/switch-screen', {
    //   method: 'POST',
    //   headers: { 'Content-Type': 'application/json' },
    //   body: JSON.stringify({ disasterType }),
    // });
  },
};
```

---

---

### constants.js - 定数定義

```javascript
// 自然災害の種類（緊急モード用）
export const DISASTER_TYPES = {
  EARTHQUAKE: 'earthquake',
  TYPHOON: 'typhoon',
  HEAVY_RAIN: 'heavy_rain',
  OTHER: 'other',
};

// 自然災害の種類（日本語）
export const DISASTER_TYPE_LABELS = {
  [DISASTER_TYPES.EARTHQUAKE]: '地震',
  [DISASTER_TYPES.TYPHOON]: '台風',
  [DISASTER_TYPES.HEAVY_RAIN]: '大雨・洪水',
  [DISASTER_TYPES.OTHER]: 'その他',
};

// 緊急度レベル
export const URGENCY_LEVELS = {
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
};

// 緊急度の色
export const URGENCY_COLORS = {
  [URGENCY_LEVELS.LOW]: '#4CAF50',
  [URGENCY_LEVELS.MEDIUM]: '#FFC107',
  [URGENCY_LEVELS.HIGH]: '#F44336',
};

// 対応状況
export const STATUS_TYPES = {
  PENDING: 'pending',
  IN_PROGRESS: 'in_progress',
  RESOLVED: 'resolved',
};

// 警備員ステータス（詳細仕様は未定）
export const SECURITY_STATUS = {
  ON_DUTY: 'on_duty',
  BREAK: 'break',
  MOVING: 'moving',
};

// カウント操作種別（表示用のみ）
export const COUNTER_OPERATIONS = {
  INCREMENT: 'increment',
  DECREMENT: 'decrement',
  RESET: 'reset',
};

// 更新間隔（ミリ秒）
export const UPDATE_INTERVALS = {
  WEATHER: 300000,  // 5分
  SECURITY: 30000,  // 30秒
  CLOCK: 1000,      // 1秒
};

// デフォルト位置（大学の座標）
export const DEFAULT_LOCATION = {
  latitude: 35.6812,
  longitude: 139.7671,
};

// 避難情報設定（自然災害のみ）
export const EVACUATION_INFO = {
  [DISASTER_TYPES.EARTHQUAKE]: {
    title: '地震発生 - 避難経路',
    description: '耐震構造の建物へ避難してください',
    routes: [], // 避難経路データ
  },
  [DISASTER_TYPES.TYPHOON]: {
    title: '台風接近 - 屋内避難',
    description: '安全な屋内へ避難してください',
    routes: [], // 屋内避難場所データ
  },
  [DISASTER_TYPES.HEAVY_RAIN]: {
    title: '大雨・洪水 - 高所避難',
    description: '高所へ避難してください',
    routes: [], // 高所避難場所データ
  },
};
```

---

## 機能要件

### デモアプリ（カウンター）

1. **カウント表示**
   - 現在のカウント値を大きく表示

2. **カウントアップ**
   - ボタンをタップでカウント+1

3. **カウントダウン**
   - ボタンをタップでカウント-1

4. **リセット**
   - カウントを0に戻す

---

### Item10機能（実長システム）

#### 1. 来場者人数カウンター機能

**概要：**
大学祭の来場者数をリアルタイムで表示・管理する機能

**詳細仕様：**
- 来場者数の表示（大きく見やすい表示）
- **来場者数は渉外部システムでカウントされたものを表示するのみ**
- カウント数は Supabase にリアルタイム保存
- 複数デバイスからの同時閲覧に対応（同期機能）
- 時間帯別の来場者数を記録
- 日別の累計来場者数を表示
- カウント履歴の確認が可能

---

#### 2. 不審者情報管理機能

**概要：**
不審者の発見情報を共有し、警備体制を強化する機能

**詳細仕様：**
- 不審者情報の新規登録（発見場所、時刻、特徴、詳細）
- 緊急度レベルの設定（低・中・高）
- 緊急度に応じた色分け表示（緑・黄・赤）
- 写真のアップロード機能（Google Drive 連携）
  - **Supabase には Google Drive のリンクのみを保存**
- 発見場所のマップ表示
リアルタイム通知機能（**ERP の通知機能を呼び出し、全スタッフへ通知**）

---

#### 3. 雨雲レーダ表示機能

**概要：**
天候情報をリアルタイムで確認し、イベント運営の判断材料を提供する機能

**詳細仕様：**
- 外部API（気象庁または天気予報API）との連携
- 現在地周辺の雨雲の動きをマップ表示
- 5分ごとの自動更新
- 1時間先までの雨雲予測表示
- 降水確率と天気予報の表示
- **1時間あたりの降水量（mm/h）を表示**
- 警報・注意報の表示
- 手動更新ボタン

---

#### 4. 時間表示機能

**概要：**
現在時刻を常時表示し、時間管理をサポートする機能

**詳細仕様：**
- デジタル時計形式で表示
- 24時間表記
- 1秒ごとの自動更新
- 大きく見やすいフォントサイズ
- 年月日も併せて表示

---

#### 5. 緊急用モード機能

**概要：**
自然災害発生時に全員へ一斉通知を送信し、適切な避難行動を促す機能

**詳細仕様：**
- 緊急モードのON/OFF切替
- ONにする際に確認ダイアログを表示
- **自然災害の種類を選択（プルダウンまたは自由記述）**
  - 地震
  - 台風
  - 大雨・洪水
  - その他（自由記述）
- **詳細メッセージ入力欄**
**緊急通知の送信（ERPの通知機能を使用し、全員にプッシュ通知）**
**通知内容：選択した災害の種類 + 詳細メッセージ** 
**緊急モード中は全員のERP画面が避難情報画面に切り替わる**

  - 自然災害の種類ごとに表示される避難経路・場所が変更
  - 地震：耐震構造の建物への避難経路
  - 台風：屋内への避難指示
  - 大雨・洪水：高所への避難経路
- 緊急モード中は画面表示が赤色に変化
- 緊急モード発動履歴の記録
- 発動者と発動時刻の記録
- 解除時にも通知を送信

**補足：**
- 緊急モードは全企画を停止する必要がある自然災害のみを対象とする

---

#### 6. 警備配置情報管理機能

**概要：**
警備員の配置場所と状況をリアルタイムで把握する機能

**詳細仕様：**
- **注意：詳細な仕様は未定（今後検討）**
- 警備員の配置場所をマップ表示
- リスト表示への切替が可能
- 各警備員の情報（氏名、配置エリア、無線チャンネル）
- 配置時刻と予定終了時刻の表示
- ステータス表示（配置中・休憩中・移動中）
- 警備員の位置情報更新（手動または自動）
- エリア別の警備員数の集計
- シフト交代のアラート機能
- **注意：** 連絡は無線で行うため、電話機能は不要

---

## 画面設計

### ホーム画面

- カウント値の表示（大きなフォント）
- カウントアップボタン（+ ボタン）
- カウントダウンボタン（- ボタン）
- リセットボタン

---

### Item10 画面（実長システム）

**画面名:** Item10Screen  
**パス/識別子:** `/item10`

**表示項目:**

1. **来場者人数カウンター（数値表示のみ）**
   - 大きな数値表示（現在の来場者数）
   - 時間帯別カウント履歴グラフ
   - 本日の累計来場者数
   - **注意：** 渉外部システムからのデータを表示するのみ

2. **不審者情報パネル（リスト形式）**
   - 不審者情報カード（最新5件を表示）
     - 発見時刻
     - 発見場所
     - 緊急度レベル（色分け表示：緑・黄・赤）
     - 対応状況（未対応・対応中・対応済み）
     - 詳細ボタン
   - 新規登録ボタン
   - 全件表示ボタン
   - 写真アップロード（Google Drive 連携）

3. **雨雲レーダ（外部API連携マップ表示）**
   - マップ表示エリア
   - 現在地マーカー
   - 雨雲アニメーション
   - 降水確率表示
   - **1時間あたりの降水量（mm/h）表示**
   - 天気予報アイコン
   - 最終更新時刻
   - 手動更新ボタン

4. **時間表示（デジタル時計）**
   - 現在時刻（HH:MM:SS形式）
   - 現在日付（YYYY/MM/DD）
   - 曜日表示

5. **緊急用モードボタン（トグルスイッチ）**
   - 緊急モードON/OFFスイッチ
   - **自然災害種別選択（プルダウン）**
     - 地震
     - 台風
     - 大雨・洪水
     - その他（自由記述可能）
   - **詳細メッセージ入力欄**
   - ステータス表示（通常モード・緊急モード）
   - 発動履歴表示ボタン

6. **警備の配置情報（マップまたはリスト表示）**
   - **注意：詳細仕様は未定（今後検討）**
   - 表示切替ボタン（マップ/リスト）
   - マップ表示時：
     - 警備員位置マーカー
     - エリア境界線
     - 各マーカーをタップで詳細表示
   - リスト表示時：
     - 警備員名
     - 配置エリア
     - ステータス（配置中・休憩中・移動中）
     - 配置時刻
     - 無線チャンネル
   - エリア別集計表示
   - **注意：** 電話機能は不要（無線で連絡）

**動作:**

1. **初期表示**
   - 画面を開くと全ての情報を Supabase から取得
   - 雨雲レーダ API から天気情報を取得
   - すべてのデータを表示

2. **来場者カウンター表示**
   - データは Supabase から自動取得して表示
   - カウント変更時にリアルタイム更新（他のユーザーが変更した場合）
   - 時間帯別履歴を自動取得して表示

3. **不審者情報操作**
   - 新規登録ボタンタップ → 登録フォーム表示 → 情報入力 → 写真は Google Drive へアップロード → Supabase に保存
   - **ERPの通知機能を呼び出して全スタッフへ通知送信**
   - Supabase のリアルタイム機能で新規登録を検知 → 自動でリスト更新
   - 詳細ボタンタップ → 不審者詳細画面へ遷移
   - 対応状況更新 → リアルタイムで全デバイスに反映

4. **雨雲レーダ操作**
   - 5分ごとに自動で API を呼び出して更新
   - 手動更新ボタンタップ → 即座に API 再取得 → 表示更新
   - マップ上をドラッグ・ピンチで拡大縮小可能
   - **降水量（mm/h）を数値で表示**

5. **時間表示**
   - 1秒ごとに JavaScript の Date オブジェクトから時刻取得 → 表示更新

6. **緊急用モード操作**
   - スイッチをタップ → 確認ダイアログ表示
   - **自然災害の種類を選択（プルダウン）**
     - 地震 / 台風 / 大雨・洪水 / その他
   - **詳細メッセージを入力**
   - 「はい」選択 → 緊急モード ON → **ERPの通知機能を呼び出して全員にプッシュ通知送信** → 画面が赤色に変化
   - **全員のERP画面が避難情報画面に自動切替**
     - 地震：耐震構造の建物への避難経路表示
     - 台風：屋内への避難指示表示
     - 大雨・洪水：高所への避難経路表示
   - 「いいえ」選択 → 何もしない
   - 発動履歴ボタンタップ → 過去の発動履歴画面へ遷移
   - 緊急モード解除 → 通常モードに戻る → 解除通知送信

7. **警備配置情報操作**
   - **注意：詳細仕様は未定（今後検討）**
   - 表示切替ボタンタップ → マップ/リスト表示を切替
   - マップ表示時：
     - マーカータップ → 警備員詳細ポップアップ表示
     - 無線チャンネル情報を表示
   - リスト表示時：
     - リストアイテムタップ → 警備員詳細画面へ遷移
     - ステータス変更ボタン → ステータス更新 → Supabase に保存
   - 警備員位置情報は30秒ごとに自動更新

**エラーハンドリング:**

- API 取得失敗時 → エラーメッセージ表示「データの取得に失敗しました」
- ネットワーク切断時 → 警告表示「オフライン状態です」
- データ保存失敗時 → エラーメッセージ表示 → 再試行ボタン表示

---

## データベース設計

### テーブル一覧

| テーブル名            | 説明                     |
| --------------------- | ------------------------ |
| visitor_counts        | 来場者カウント情報       |
| suspicious_persons    | 不審者情報               |
| emergency_logs        | 緊急モード発動履歴       |
| security_placements   | 警備員配置情報           |
| security_members      | 警備員マスタ情報         |

---

### visitor_counts テーブル

**説明:** 来場者数のカウント情報を時系列で管理

| カラム名      | 型          | 制約             | 説明                         |
| ------------- | ----------- | ---------------- | ---------------------------- |
| id            | uuid        | PK               | レコード ID                  |
| count         | integer     | NOT NULL         | 来場者数                     |
| counted_at    | timestamptz | NOT NULL         | カウント日時                 |
| operation     | text        | NOT NULL         | 操作種別（increment/decrement/reset） |
| updated_by    | uuid        | FK (users)       | 更新者 ID                    |
| created_at    | timestamptz | NOT NULL         | 作成日時                     |

**リレーション:**
- `updated_by` → `users.id`

**インデックス:**
- `counted_at` に対するインデックス（時系列クエリの高速化）

---

### suspicious_persons テーブル

**説明:** 不審者情報を管理

| カラム名         | 型          | 制約             | 説明                                      |
| ---------------- | ----------- | ---------------- | ----------------------------------------- |
| id               | uuid        | PK               | 不審者情報 ID                             |
| discovered_at    | timestamptz | NOT NULL         | 発見日時                                  |
| location         | text        | NOT NULL         | 発見場所                                  |
| description      | text        |                  | 詳細説明                                  |
| urgency_level    | text        | NOT NULL         | 緊急度（low/medium/high）                |
| status           | text        | NOT NULL         | 対応状況（pending/in_progress/resolved） |
| photo_url        | text        |                  | **Google Drive の共有リンク**             |
| latitude         | float       |                  | 緯度                                      |
| longitude        | float       |                  | 経度                                      |
| reported_by      | uuid        | FK (users)       | 報告者 ID                                 |
| resolved_at      | timestamptz |                  | 承認日時                              |
| created_at       | timestamptz | NOT NULL         | 作成日時                                  |
| updated_at       | timestamptz | NOT NULL         | 更新日時                                  |

**リレーション:**
- `reported_by` → `users.id`

**インデックス:**
- `discovered_at`, `urgency_level`, `status` に対するインデックス

**補足：**
- 写真は Google Drive にアップロードし、Supabase には共有リンクのみを保存

---

### emergency_logs テーブル

**説明:** 緊急モードの発動・解除履歴を管理

| カラム名      | 型          | 制約             | 説明                           |
| ------------- | ----------- | ---------------- | ------------------------------ |
| id            | uuid        | PK               | ログ ID                        |
| action        | text        | NOT NULL         | アクション（activate/deactivate） |
| activated_by  | uuid        | FK (users)       | 発動者 ID                      |
| emergency_type| text        |                  | 自然災害の種類（地震/台風/大雨・洪水/その他） |
| reason        | text        |                  | 詳細メッセージ                 |
| created_at    | timestamptz | NOT NULL         | 実行日時                       |

**リレーション:**
- `activated_by` → `users.id`

**インデックス:**
- `created_at` に対するインデックス

---

### security_placements テーブル

**説明:** 警備員の配置情報を管理

**注意：詳細仕様は未定（今後検討）**

| カラム名         | 型          | 制約                  | 説明                                      |
| ---------------- | ----------- | --------------------- | ----------------------------------------- |
| id               | uuid        | PK                    | 配置 ID                                   |
| security_member_id | uuid      | FK (security_members) | 警備員 ID                                 |
| area             | text        | NOT NULL              | 配置エリア                                |
| status           | text        | NOT NULL              | ステータス（on_duty/break/moving）       |
| latitude         | float       |                       | 現在位置（緯度）                          |
| longitude        | float       |                       | 現在位置（経度）                          |
| start_time       | timestamptz | NOT NULL              | 配置開始時刻                              |
| end_time         | timestamptz |                       | 配置終了時刻                              |
| created_at       | timestamptz | NOT NULL              | 作成日時                                  |
| updated_at       | timestamptz | NOT NULL              | 更新日時                                  |

**リレーション:**
- `security_member_id` → `security_members.id`

**インデックス:**
- `security_member_id`, `status`, `start_time` に対するインデックス

---

### security_members テーブル

**説明:** 警備員のマスタ情報を管理

| カラム名      | 型          | 制約             | 説明           |
| ------------- | ----------- | ---------------- | -------------- |
| id            | uuid        | PK               | 警備員 ID      |
| name          | text        | NOT NULL         | 氏名           |
| radio_channel | text        |                  | 無線チャンネル |
| email         | text        |                  | メールアドレス |
| created_at    | timestamptz | NOT NULL         | 作成日時       |
| updated_at    | timestamptz | NOT NULL         | 更新日時       |

**リレーション:**
- なし

---

## API 設計

### 1. Supabase REST API

**アクセス方法:**

Item10 システムが Supabase に直接アクセスする場合

**ベース URL:**

```
https://{PROJECT_ID}.supabase.co/rest/v1
```

**認証方法:**

以下の HTTP ヘッダーを付ける

```
apikey: {SUPABASE_ANON_KEY}
Authorization: Bearer {SUPABASE_ANON_KEY}
Content-Type: application/json
```

**必要な情報:**

- `PROJECT_ID`: Supabase プロジェクト設定から取得
- `SUPABASE_ANON_KEY`: Supabase プロジェクト設定 > API > Project API keys から取得

---

#### 来場者カウント関連 API

##### 現在の来場者数取得

```
GET /visitor_counts?select=*&order=counted_at.desc&limit=1
```

**レスポンス:**

```json
[
  {
    "id": "uuid",
    "count": 1234,
    "counted_at": "2026-02-04T08:00:00Z",
    "operation": "increment",
    "updated_by": "uuid",
    "created_at": "2026-02-04T08:00:00Z"
  }
]
```

---

##### 来場者カウント更新

```
POST /visitor_counts
```

**リクエストボディ:**

```json
{
  "count": 1235,
  "counted_at": "2026-02-04T08:05:00Z",
  "operation": "increment",
  "updated_by": "user-uuid"
}
```

**レスポンス:**

```json
{
  "id": "uuid",
  "count": 1235,
  "counted_at": "2026-02-04T08:05:00Z",
  "operation": "increment",
  "updated_by": "user-uuid",
  "created_at": "2026-02-04T08:05:00Z"
}
```

---

##### 時間帯別来場者数取得

```
GET /visitor_counts?select=*&counted_at=gte.2026-02-04T00:00:00Z&counted_at=lte.2026-02-04T23:59:59Z&order=counted_at.asc
```

**レスポンス:**

```json
[
  {
    "id": "uuid",
    "count": 100,
    "counted_at": "2026-02-04T09:00:00Z",
    "operation": "increment"
  },
  {
    "id": "uuid",
    "count": 250,
    "counted_at": "2026-02-04T10:00:00Z",
    "operation": "increment"
  }
]
```

---

#### 不審者情報関連 API

##### 不審者情報一覧取得

```
GET /suspicious_persons?select=*&order=discovered_at.desc&limit=5
```

**レスポンス:**

```json
[
  {
    "id": "uuid",
    "discovered_at": "2026-02-04T08:00:00Z",
    "location": "A棟前",
    "description": "不審な行動を確認",
    "urgency_level": "high",
    "status": "pending",
    "photo_url": "https://...",
    "latitude": 35.6812,
    "longitude": 139.7671,
    "reported_by": "uuid",
    "created_at": "2026-02-04T08:00:00Z"
  }
]
```

---

##### 不審者情報登録

```
POST /suspicious_persons
```

**リクエストボディ:**

```json
{
  "discovered_at": "2026-02-04T08:00:00Z",
  "location": "A棟前",
  "description": "不審な行動を確認",
  "urgency_level": "high",
  "status": "pending",
  "latitude": 35.6812,
  "longitude": 139.7671,
  "reported_by": "user-uuid"
}
```

**レスポンス:**

```json
{
  "id": "uuid",
  "discovered_at": "2026-02-04T08:00:00Z",
  "location": "A棟前",
  "description": "不審な行動を確認",
  "urgency_level": "high",
  "status": "pending",
  "created_at": "2026-02-04T08:00:00Z"
}
```

---

##### 不審者情報ステータス更新

```
PATCH /suspicious_persons?id=eq.{ID}
```

**リクエストボディ:**

```json
{
  "status": "resolved",
  "resolved_at": "2026-02-04T09:00:00Z"
}
```

---

#### 緊急ログ関連 API

##### 緊急モード発動

```
POST /emergency_logs
```

**リクエストボディ:**

```json
{
  "action": "activate",
  "activated_by": "user-uuid",
  "reason": "緊急事態発生"
}
```

**レスポンス:**

```json
{
  "id": "uuid",
  "action": "activate",
  "activated_by": "user-uuid",
  "reason": "緊急事態発生",
  "created_at": "2026-02-04T08:00:00Z"
}
```

---

##### 緊急モード履歴取得

```
GET /emergency_logs?select=*&order=created_at.desc&limit=10
```

**レスポンス:**

```json
[
  {
    "id": "uuid",
    "action": "activate",
    "activated_by": "user-uuid",
    "reason": "緊急事態発生",
    "created_at": "2026-02-04T08:00:00Z"
  }
]
```

---

#### 警備配置情報関連 API

##### 警備員配置情報一覧取得

```
GET /security_placements?select=*,security_members(*)&status=eq.on_duty
```

**レスポンス:**

```json
[
  {
    "id": "uuid",
    "security_member_id": "uuid",
    "area": "A棟エリア",
    "status": "on_duty",
    "latitude": 35.6812,
    "longitude": 139.7671,
    "start_time": "2026-02-04T08:00:00Z",
    "security_members": {
      "id": "uuid",
      "name": "山田太郎",
      "phone_number": "090-1234-5678",
      "email": "yamada@example.com"
    }
  }
]
```

---

##### 警備員ステータス更新

```
PATCH /security_placements?id=eq.{ID}
```

**リクエストボディ:**

```json
{
  "status": "break",
  "latitude": 35.6812,
  "longitude": 139.7671
}
```

---

##### エリア別警備員数集計

```
GET /security_placements?select=area&status=eq.on_duty
```

**レスポンス:**

```json
[
  {
    "area": "A棟エリア"
  },
  {
    "area": "B棟エリア"
  },
  {
    "area": "A棟エリア"
  }
]
```

（フロントエンドで集計処理を実行）

---

### 2. 外部 API（天気情報）

**アクセス方法:**

気象庁または天気予報 API サービスを利用

**推奨 API:**
- OpenWeatherMap API
- 気象庁 API（無料）

**ベース URL（OpenWeatherMap の場合）:**

```
https://api.openweathermap.org/data/2.5
```

---

#### 現在の天気取得

```
GET /weather?lat={latitude}&lon={longitude}&appid={API_KEY}&units=metric&lang=ja
```

**パラメータ:**
| 名前 | 型 | 必須 | 説明 |
|-----|-----|-----|------|
| lat | float | Yes | 緯度 |
| lon | float | Yes | 経度 |
| appid | string | Yes | API キー |
| units | string | No | 単位（metric=摂氏） |
| lang | string | No | 言語（ja=日本語） |

**レスポンス:**

```json
{
  "weather": [
    {
      "main": "Rain",
      "description": "小雨"
    }
  ],
  "main": {
    "temp": 15.5,
    "humidity": 80
  },
  "rain": {
    "1h": 2.5
  }
}
```

---

#### 雨雲レーダ（降水予測）

```
GET /forecast?lat={latitude}&lon={longitude}&appid={API_KEY}&units=metric&lang=ja
```

**レスポンス:**

```json
{
  "list": [
    {
      "dt": 1707033600,
      "main": {
        "temp": 15.5
      },
      "weather": [
        {
          "main": "Rain",
          "description": "小雨"
        }
      ],
      "pop": 0.8
    }
  ]
}
```

---

### 3. プッシュ通知 API

**アクセス方法:**

Expo Push Notification サービスを利用

**ベース URL:**

```
https://exp.host/--/api/v2/push/send
```

---

#### 緊急通知送信

```
POST /push/send
```

**リクエストボディ:**

```json
{
  "to": ["ExponentPushToken[xxx]", "ExponentPushToken[yyy]"],
  "title": "緊急通知",
  "body": "緊急事態が発生しました。至急確認してください。",
  "sound": "default",
  "priority": "high",
  "data": {
    "type": "emergency",
    "timestamp": "2026-02-04T08:00:00Z"
  }
}
```

**レスポンス:**

```json
{
  "data": [
    {
      "status": "ok",
      "id": "notification-id-1"
    },
    {
      "status": "ok",
      "id": "notification-id-2"
    }
  ]
}
```

---

## 開発の進め方

1. このテンプレートをフォーク
2. プロジェクト名を変更
3. Supabase プロジェクトを作成し、環境変数を設定
4. 必要に応じてデータベース設計
5. 機能を追加開発

---

## セットアップ手順

1. リポジトリをクローン
   ```bash
   git clone [repository-url]
   cd react-native-expo-template-2026
   ```

2. 依存関係をインストール
   ```bash
   npm install
   ```

3. 環境変数を設定
   ```bash
   cp .env.example .env
   # .env ファイルを編集してSupabaseの認証情報を設定
   ```

4. アプリを起動
   ```bash
   npm start
   ```

---

## ライセンス

MIT License

